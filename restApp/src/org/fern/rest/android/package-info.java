/**
 * This is the main package for the Android application.  The direct classes are
 * either classes that directly relate to all other packages or classes that 
 * didn't seem to fit well in any other package.
 */
package org.fern.rest.android;
